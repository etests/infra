apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: etests-django
  annotations:
    kubernetes.io/ingress.global-static-ip-name: etests-django
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: etests.co.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: etests-django
                port:
                  number: 80
